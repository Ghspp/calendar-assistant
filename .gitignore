node_modules/
dist/
dist-ssr/
coverage/
*.local

# Environment — the Google OAuth client id goes here in Stage 3.
# It is not a secret (it ships in the browser bundle), but it is machine-specific.
.env
.env.*
!.env.example

# Claude Code local settings — machine specific.
.claude/settings.local.json

# Editors
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
